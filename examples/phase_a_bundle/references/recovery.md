# Recovery guide

This guide contains mutually exclusive recovery branches.

## When validation fails

Always collect the validation code and preserve the failing field name. Always collect the validation code and preserve the failing field name. Always collect the validation code and preserve the failing field name. Always collect the validation code and preserve the failing field name. Always collect the validation code and preserve the failing field name. Always collect the validation code and preserve the failing field name. Always collect the validation code and preserve the failing field name. Always collect the validation code and preserve the failing field name. Always collect the validation code and preserve the failing field name. Always collect the validation code and preserve the failing field name. Always collect the validation code and preserve the failing field name. Always collect the validation code and preserve the failing field name.

## When the source is unavailable

Always record the source URI and return a recoverable diagnostic. Always record the source URI and return a recoverable diagnostic. Always record the source URI and return a recoverable diagnostic. Always record the source URI and return a recoverable diagnostic. Always record the source URI and return a recoverable diagnostic. Always record the source URI and return a recoverable diagnostic. Always record the source URI and return a recoverable diagnostic. Always record the source URI and return a recoverable diagnostic. Always record the source URI and return a recoverable diagnostic. Always record the source URI and return a recoverable diagnostic. Always record the source URI and return a recoverable diagnostic. Always record the source URI and return a recoverable diagnostic.
